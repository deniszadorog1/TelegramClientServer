<UserControl x:Class="TelegramVisualPart.UserControls.SettingsControls.LanguageControls.LangRadioBut"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:TelegramVisualPart.UserControls.SettingsControls.LanguageControls"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="450"
             Height="60"
             MouseEnter="UserControl_MouseEnter"
             MouseLeave="UserControl_MouseLeave">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <RadioButton Grid.Column="0"
                     VerticalAlignment="Center" 
                     HorizontalAlignment="Center"
                     materialDesign:RadioButtonAssist.RadioButtonSize="28"
                     Background="{StaticResource TempActiveTextColor}"/>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Name ="LangNativeName"
                       Text="Native lang name" VerticalAlignment="Bottom"
                       Margin="0 0 0 1" FontSize="16"
                       Foreground="{StaticResource UsualTextColor}"/>
            <TextBlock Grid.Row="1" Name ="LangEngName" 
                       Text="Eng lang name" Foreground="Gray"
                       VerticalAlignment="Top" FontSize="16"
                       Margin="0 1 0 0"/>

        </Grid>
    </Grid>
</UserControl>
