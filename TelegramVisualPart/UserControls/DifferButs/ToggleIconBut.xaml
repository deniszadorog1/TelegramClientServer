<UserControl x:Class="TelegramVisualPart.UserControls.ToggleIconBut"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TelegramVisualPart.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="500" 
             MouseEnter="UserControl_MouseEnter"
             MouseLeave="UserControl_MouseLeave"
             MouseDown="UserControl_MouseDown">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="75"/>
        </Grid.ColumnDefinitions>

        <materialDesign:PackIcon Kind="ProgressClock"  Name="Icon"
                                 Width="25" Height="25" 
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Center"
                                 Foreground="{StaticResource UsualTextColor}"/>

        <TextBlock Name ="TextBlock" Grid.Column="1" Text="there gonna be a text" 
                   VerticalAlignment="Center" Foreground="{StaticResource UsualTextColor}" FontSize="18"
                   Margin="10" FontWeight="Normal"/>

        <ToggleButton Name ="Toggle" Grid.Column="2" Width="40" Height="40"
                      HorizontalAlignment="Left" 
                      Style="{StaticResource MaterialDesignSwitchDarkToggleButton}"
                      ToolTip="{x:Null}"
                      Checked="Toggle_Checked"
                      Unchecked="Toggle_Unchecked"
                      IsChecked="True" Background="{StaticResource TempActiveTextColor}"/>
    </Grid>
</UserControl>
