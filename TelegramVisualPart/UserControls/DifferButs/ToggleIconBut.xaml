<UserControl x:Class="TelegramVisualPart.UserControls.ToggleIconBut"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TelegramVisualPart.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="75"/>
        </Grid.ColumnDefinitions>

        <materialDesign:PackIcon Kind="ProgressClock"  Name="Icon"
                                 Width="30" Height="30" 
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Right"
                                 Foreground="LightGray"/>

        <TextBlock Name ="TextBlock" Grid.Column="1" Text="there gonna be a text" 
                   VerticalAlignment="Center" Foreground="LightGray" FontSize="14"
                   Margin="10" FontWeight="DemiBold"/>

        <ToggleButton Name ="Toggle" Grid.Column="2" Width="35"
                      HorizontalAlignment="Left" 
              Style="{StaticResource MaterialDesignSwitchDarkToggleButton}"
              ToolTip="MaterialDesignSwitchDarkToggleButton"                      
              IsChecked="True" Background="{StaticResource TempActiveTextColor}"/>

      
    </Grid>
</UserControl>
