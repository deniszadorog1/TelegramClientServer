<UserControl x:Class="TelegramVisualPart.UserControls.IconTextBut"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TelegramVisualPart.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200" 
             
             MouseEnter="UserControl_MouseEnter"
             MouseLeave="UserControl_MouseLeave">
    <!--="5" -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <materialDesign:PackIcon Kind="ProgressDownload" 
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Center"
                                 Foreground="{StaticResource TempActiveTextColor}"
                                 Width="35" Height="35"
                                 Name="IconType"/>
        <TextBlock Grid.Column="1" Name="ButName" Text="There would be a but name"
                   VerticalAlignment="Center" HorizontalAlignment="Left"
                   Foreground="{StaticResource TempActiveTextColor}" Margin="5 0 0 0"/>
        
    </Grid>
</UserControl>
