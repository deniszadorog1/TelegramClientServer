<UserControl x:Class="TelegramVisualPart.UserControls.ChatsSearch.SearchMediaMenuFolder.SearchMediaMenuItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TelegramVisualPart.UserControls.ChatsSearch.SearchMediaMenuFolder"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200"
             Background="Transparent"
             MouseEnter="UserControl_MouseEnter" 
             MouseLeave="UserControl_MouseLeave">
    <Grid Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <materialDesign:PackIcon Name="Icon" 
                                 Grid.Column="0"
                                 Kind="Anonymous" 
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Center"
                                 Height="25" Width="25"
                                 Foreground="White"/>

        <TextBlock Name="ItemTextBlock" 
                   Grid.Column="1"
                   VerticalAlignment="Center"
                   Text="Basic text"
                   Foreground="White"
                   FontFamily="{StaticResource AppFont}"
                   FontSize="16"/>

    </Grid>
</UserControl>
