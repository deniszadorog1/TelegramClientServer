<UserControl x:Class="TelegramVisualPart.UserControls.ChatControls.ChatButsControls.SentObjects.SharedFile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TelegramVisualPart.UserControls.ChatControls.ChatButsControls.SentObjects"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="400"
             Height="150">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Margin="15" CornerRadius="15" 
                Background="Red">

        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="30"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="FileName" Text="There gona be file name"
                       Foreground="White" VerticalAlignment="Center"
                       FontSize="16" Grid.Row="0" Margin="0 15 0 0"/>

            <TextBlock x:Name="FileSize" Text="There gona be file size"
                       Foreground="Gray" VerticalAlignment="Center"
                       FontSize="14" Grid.Row="1"/>

            <TextBlock x:Name="SentDate" Text="There gona be file name"
                       Foreground="Gray" VerticalAlignment="Center"
                       FontSize="14" Grid.Row="2"/>

        </Grid>
    </Grid>
</UserControl>
