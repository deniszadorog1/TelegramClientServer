<UserControl x:Class="TelegramVisualPart.UserControls.ChatControls.ChatButsControls.SentObjects.DateStackPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TelegramVisualPart.UserControls.ChatControls.ChatButsControls.SentObjects"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <Border Name="BgBorder" CornerRadius="15"
            Background="{StaticResource DarkThemeOne}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Name="DateBlock" Text="There gona be date"
                   VerticalAlignment="Center" Margin="15" Foreground="White"
                   FontSize="14"/>
            <StackPanel Grid.Row="1" Name="WrapPanel"
                        Orientation="Vertical">

            </StackPanel>
        </Grid>
    </Border>
</UserControl>
