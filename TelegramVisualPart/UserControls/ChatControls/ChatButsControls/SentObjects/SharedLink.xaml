<UserControl x:Class="TelegramVisualPart.UserControls.ChatControls.ChatButsControls.SentObjects.SharedLink"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TelegramVisualPart.UserControls.ChatControls.ChatButsControls.SentObjects"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="400"
             Height="150">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#3FBBAB" CornerRadius="15" Margin="10">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center"
                       Text="VIS" Foreground="White" FontSize="36"/>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Name="LinkName" TextWrapping="Wrap"
                       VerticalAlignment="Center" Foreground="White"
                       FontSize="16" Text="There gonna be file link name"/>

            <TextBlock Grid.Row="1" Name="Link" TextWrapping="Wrap"
                       VerticalAlignment="Center" Foreground="White"
                       FontSize="16" Text="There gonna be Link to site"/>
        </Grid>
    </Grid>
</UserControl>
