<UserControl x:Class="TelegramVisualPart.UserControls.ChatControls.ChatButsControls.TextBoxMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TelegramVisualPart.UserControls.ChatControls.ChatButsControls"
             xmlns:But="clr-namespace:TelegramVisualPart.UserControls.ChatControls.ChatButsControls"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="200"
             Width="200"
             >
    <Border CornerRadius="10"
            Background="{StaticResource DarkThemeOne}">
        <StackPanel Margin="5">
            <But:TextBoxMenuButton x:Name="Undo" PreviewMouseDown="Button_PreviewMouseDown"/>
            <But:TextBoxMenuButton x:Name="Redo" PreviewMouseDown="Button_PreviewMouseDown"/>
            <But:TextBoxMenuButton x:Name="Cut" PreviewMouseDown="Button_PreviewMouseDown"/>
            <But:TextBoxMenuButton x:Name="Copy" PreviewMouseDown="Button_PreviewMouseDown"/>
            <But:TextBoxMenuButton x:Name="Paste" PreviewMouseDown="Button_PreviewMouseDown"/>
            <But:TextBoxMenuButton x:Name="Delete" PreviewMouseDown="Button_PreviewMouseDown"/>
            <But:TextBoxMenuButton x:Name="SelectAll" PreviewMouseDown="Button_PreviewMouseDown"/>
        </StackPanel>
    </Border>
</UserControl>
