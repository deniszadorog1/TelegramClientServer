<UserControl x:Class="TelegramVisualPart.UserControls.ChatControls.ChatButsControls.UserChatMenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TelegramVisualPart.UserControls.ChatControls.ChatButsControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400"
             Height="35"
             MouseEnter="UserControl_MouseEnter"
             MouseLeave="UserControl_MouseLeave">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>

        <materialDesign:PackIcon Kind="ProgressDownload" Margin="15 0 0 0"
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Center"
                                 Foreground="{StaticResource UsualTextColor}"
                                 Width="25" Height="25"
                                 Name="IconType"/>

        <TextBlock Grid.Column="1" Name="ButName" Text="There would be a but name"
                   VerticalAlignment="Center" HorizontalAlignment="Left"
                   Foreground="{StaticResource UsualTextColor}" Margin="15 0 0 0" FontSize="14"/>


    </Grid>
</UserControl>
