<UserControl x:Class="TelegramVisualPart.UserControls.ChatControls.ChatMessages.SelectionTick"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TelegramVisualPart.UserControls.ChatControls.ChatMessages"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="30"
             
             Background="Transparent"
             
             MouseEnter="UserControl_MouseEnter"
             MouseLeave="UserControl_MouseLeave"
             PreviewMouseDown="UserControl_PreviewMouseDown">

    <Grid>
        <Ellipse Name="ChooseEllipse" Stroke="Black" StrokeThickness="1"             
             Fill="{StaticResource TempActiveTextColor}">
        </Ellipse>

        <materialDesign:PackIcon Kind="Tick"
                                 Name="ChosenTickIcon"
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Center"
                                 Foreground="{StaticResource UsualTextColor}"/>
    </Grid>
</UserControl>
